name: currency_textfield_2
description: A flutter package that implements a Controller for currency text input.
version: 5.1.0
homepage: https://github.com/Macacoazul01/currency_textfield
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
